on:
  pull_request: {}
  push:
    paths:
      - 'code/**'
    branches: [ main ]

jobs:
  build:
    uses: ./.github/workflows/build.yml
    secrets: inherit

  deploy:
    needs: [ build ]
    uses: ./.github/workflows/deploy.yml
    secrets: inherit